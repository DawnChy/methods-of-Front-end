const handleObject = {
  /**
   * @description 判断对象是否为空
   * @param {Object} obj 待判断对象
   * @returns {Boolean} true && false
   */
  isEmpty: obj => {
    let t;
    for (t in obj) {
      return false;
    }
    return true;
  },
  
  /**
   * @description 
   * @param {}  
   * @returns {} 
   */
}
